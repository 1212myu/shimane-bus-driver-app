# 運賃計算カウンター v4 修正仕様

## 修正1: 名称変更「精算」→「計算」

UIに表示されている「精算」をすべて「計算」に変更する。
これはリアルタイム決済ではなく、料金箱の現金を数える際の計算補助機能であるため。

### 変更対象
- `index.html`: ボタン表示「精算カウンター」→「計算カウンター」、タブ「精算」→「計算」
- `js/app.js`: コメントのみ（動作への影響なし）
- `js/fare-counter.js`: UIテキストの変更（精算→計算）
- `css/style.css`: クラス名はそのまま（内部的な名前なので変更不要）

## 修正2: 回数券＋現金の合算払い対応

### 背景
乗客が回数券と現金を組み合わせて運賃を支払うケースがある。
例: 350円の運賃を、200円回数券 + 150円現金で支払う。
現在の仕様では支払方法を1つしか選べないため、このケースに対応できない。

### 実装方針（シンプルに保つ）

「合算」ボタンを追加する。

#### 操作フロー
1. ドライバーが回数券を選択し、200円をタップ → 1人、回数券200円として記録
2. 「合算」ボタンをタップ（合算モードON）
3. 現金を選択し、150円をタップ → 人数は加算しない、現金150円のみ加算
4. 合算モードは1回の記録後に自動解除

#### 追加する運賃ボタン
合算払いでは端数の金額が発生するため、以下の金額ボタンを追加:
- 100円、150円

最終的なボタン配列: 100, 150, 200, 250, 300, 350, 400, 500, 550

#### UI
- 「合算」ボタンは「取消」ボタンの左横に配置
- 合算モード中は「合算」ボタンの色を変えて視覚的に分かるようにする
- 合算モード中はステータスバーに「合算入力中」と表示

#### 記録の扱い
- 合算モードで入力された記録は、人数カウントに加算しない
- 金額は通常通り該当する支払方法の合計に加算
- 取消は通常通り直前の記録を削除

## 注意
- sw.js の CACHE_NAME を v14 にインクリメント
- 修正後 git commit して push する
